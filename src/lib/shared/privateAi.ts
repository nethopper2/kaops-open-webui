// Shared Private AI constants for frontend
// If backend needs these, mirror values in a backend constants module.

export const PRIVATE_AI_MODEL_PREFIX = 'private-ai';

// This is considered the default Model prefix which a user would select.
export const PRIVATE_AI_DEFAULT_MODEL_PREFIX = 'private-ai-agent-rag';
